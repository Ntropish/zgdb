{
  "name": "@tsmk/root",
  "version": "0.1.0",
  "private": true,
  "description": "",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test:standalone": "pnpm -r test",
    "test:types": "pnpm -r test:types",
    "test": "run-p build test:standalone",
    "test:all": "run-p build test:standalone test:types"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
