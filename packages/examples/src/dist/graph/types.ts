// Generated by @zgdb/cli
import { z } from 'zod';
import { userNode, postNode, tagNode } from '../../graph.config';

export const UserSchema = userNode.extend({
  posts: z.array(z.lazy(() => PostSchema)),
});
export type User = z.infer<typeof UserSchema>;

export const PostSchema = postNode.extend({
  author: z.lazy(() => UserSchema),
  tags: z.array(z.lazy(() => TagSchema)),
});
export type Post = z.infer<typeof PostSchema>;

export const TagSchema = tagNode.extend({
  posts: z.lazy(() => PostSchema),
});
export type Tag = z.infer<typeof TagSchema>;
