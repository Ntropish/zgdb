// Generated by @zgdb/cli
import { z } from 'zod';
import type { userNode, postNode, tagNode } from '../../graph.config';

// Forward-declare all interfaces to handle circular dependencies
export interface User {}
export interface Post {}
export interface Tag {}

type BaseUser = z.infer<typeof userNode>;

export type UserCreationInput = BaseUser & {

};

export interface User extends BaseUser {
  posts: Post[];
}

type BasePost = z.infer<typeof postNode>;

export type PostCreationInput = BasePost & {
  author: BaseUser;
};

export interface Post extends BasePost {
  author: User;
  tags: Tag[];
}

type BaseTag = z.infer<typeof tagNode>;

export type TagCreationInput = BaseTag & {

};

export interface Tag extends BaseTag {
  posts: Post;
}
