// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
export class Course {
    bb = null;
    bb_pos = 0;
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsCourse(bb, obj) {
        return (obj || new Course()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsCourse(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new Course()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    id(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    title(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    courseCode(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    credits() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readInt32(this.bb_pos + offset) : 0;
    }
    departmentIds(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    departmentIdsLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    professorIds(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    professorIdsLength() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    enrollmentsIds(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    enrollmentsIdsLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    createdAt() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    updatedAt() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    static startCourse(builder) {
        builder.startObject(9);
    }
    static addId(builder, idOffset) {
        builder.addFieldOffset(0, idOffset, 0);
    }
    static addTitle(builder, titleOffset) {
        builder.addFieldOffset(1, titleOffset, 0);
    }
    static addCourseCode(builder, courseCodeOffset) {
        builder.addFieldOffset(2, courseCodeOffset, 0);
    }
    static addCredits(builder, credits) {
        builder.addFieldInt32(3, credits, 0);
    }
    static addDepartmentIds(builder, departmentIdsOffset) {
        builder.addFieldOffset(4, departmentIdsOffset, 0);
    }
    static createDepartmentIdsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startDepartmentIdsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addProfessorIds(builder, professorIdsOffset) {
        builder.addFieldOffset(5, professorIdsOffset, 0);
    }
    static createProfessorIdsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startProfessorIdsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addEnrollmentsIds(builder, enrollmentsIdsOffset) {
        builder.addFieldOffset(6, enrollmentsIdsOffset, 0);
    }
    static createEnrollmentsIdsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startEnrollmentsIdsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addCreatedAt(builder, createdAt) {
        builder.addFieldInt64(7, createdAt, BigInt('0'));
    }
    static addUpdatedAt(builder, updatedAt) {
        builder.addFieldInt64(8, updatedAt, BigInt('0'));
    }
    static endCourse(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // id
        return offset;
    }
    static createCourse(builder, idOffset, titleOffset, courseCodeOffset, credits, departmentIdsOffset, professorIdsOffset, enrollmentsIdsOffset, createdAt, updatedAt) {
        Course.startCourse(builder);
        Course.addId(builder, idOffset);
        Course.addTitle(builder, titleOffset);
        Course.addCourseCode(builder, courseCodeOffset);
        Course.addCredits(builder, credits);
        Course.addDepartmentIds(builder, departmentIdsOffset);
        Course.addProfessorIds(builder, professorIdsOffset);
        Course.addEnrollmentsIds(builder, enrollmentsIdsOffset);
        Course.addCreatedAt(builder, createdAt);
        Course.addUpdatedAt(builder, updatedAt);
        return Course.endCourse(builder);
    }
}
