// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
export class Post {
    bb = null;
    bb_pos = 0;
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPost(bb, obj) {
        return (obj || new Post()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPost(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new Post()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    id(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    title(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    content(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    published() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    viewCount() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readInt32(this.bb_pos + offset) : 0;
    }
    tagsIds(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    tagsIdsLength() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    authorIds(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    authorIdsLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    createdAt() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    updatedAt() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    static startPost(builder) {
        builder.startObject(9);
    }
    static addId(builder, idOffset) {
        builder.addFieldOffset(0, idOffset, 0);
    }
    static addTitle(builder, titleOffset) {
        builder.addFieldOffset(1, titleOffset, 0);
    }
    static addContent(builder, contentOffset) {
        builder.addFieldOffset(2, contentOffset, 0);
    }
    static addPublished(builder, published) {
        builder.addFieldInt8(3, +published, +false);
    }
    static addViewCount(builder, viewCount) {
        builder.addFieldInt32(4, viewCount, 0);
    }
    static addTagsIds(builder, tagsIdsOffset) {
        builder.addFieldOffset(5, tagsIdsOffset, 0);
    }
    static createTagsIdsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startTagsIdsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addAuthorIds(builder, authorIdsOffset) {
        builder.addFieldOffset(6, authorIdsOffset, 0);
    }
    static createAuthorIdsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startAuthorIdsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addCreatedAt(builder, createdAt) {
        builder.addFieldInt64(7, createdAt, BigInt('0'));
    }
    static addUpdatedAt(builder, updatedAt) {
        builder.addFieldInt64(8, updatedAt, BigInt('0'));
    }
    static endPost(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // id
        return offset;
    }
    static createPost(builder, idOffset, titleOffset, contentOffset, published, viewCount, tagsIdsOffset, authorIdsOffset, createdAt, updatedAt) {
        Post.startPost(builder);
        Post.addId(builder, idOffset);
        Post.addTitle(builder, titleOffset);
        Post.addContent(builder, contentOffset);
        Post.addPublished(builder, published);
        Post.addViewCount(builder, viewCount);
        Post.addTagsIds(builder, tagsIdsOffset);
        Post.addAuthorIds(builder, authorIdsOffset);
        Post.addCreatedAt(builder, createdAt);
        Post.addUpdatedAt(builder, updatedAt);
        return Post.endPost(builder);
    }
}
