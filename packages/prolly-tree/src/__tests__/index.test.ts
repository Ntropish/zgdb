import { describe, it, expect } from "vitest";
import { ProllyTree } from "../index";

describe("prolly-tree", () => {
  it("should be defined", () => {
    expect(ProllyTree).toBeDefined();
  });
});
